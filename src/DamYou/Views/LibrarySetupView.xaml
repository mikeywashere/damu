<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:DamYou.ViewModels"
             x:Class="DamYou.Views.LibrarySetupModal"
             x:DataType="vm:LibrarySetupViewModel"
             Title="Manage Photo Folders"
             BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Black}}">

    <Grid RowDefinitions="Auto,*,Auto" Padding="24">

        <!-- Header -->
        <VerticalStackLayout Grid.Row="0" Spacing="8" Margin="0,0,0,16">
            <Label Text="Manage Photo Folders"
                   FontSize="24"
                   FontAttributes="Bold"
                   HorizontalOptions="Start" />
            <Label Text="Add or remove folders. Check 'Index Again' to re-scan selected folders."
                   FontSize="13"
                   HorizontalOptions="Start"
                   Opacity="0.6" />
        </VerticalStackLayout>

        <!-- Folder list with "Index Again" checkboxes -->
        <VerticalStackLayout Grid.Row="1" Spacing="0">
            <!-- Empty state -->
            <VerticalStackLayout IsVisible="{Binding SelectedFolders.Count, Converter={StaticResource IntToBoolConverter}, ConverterParameter=invert}"
                                 HorizontalOptions="Center"
                                 VerticalOptions="Center"
                                 Spacing="12"
                                 Margin="0,40,0,0">
                <Label Text="No folders added yet."
                       FontSize="15"
                       HorizontalOptions="Center"
                       Opacity="0.5" />
                <Label Text="Click 'Add Folder' to get started."
                       FontSize="13"
                       HorizontalOptions="Center"
                       Opacity="0.4" />
            </VerticalStackLayout>

            <!-- Folder items with checkbox -->
            <CollectionView ItemsSource="{Binding SelectedFolders}"
                            IsVisible="{Binding SelectedFolders.Count, Converter={StaticResource IntToBoolConverter}}">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="x:String">
                        <Grid ColumnDefinitions="Auto,*,Auto"
                              Padding="12,10"
                              Margin="0,2">
                            <Grid.Background>
                                <SolidColorBrush Color="{AppThemeBinding Light=#F5F5F5, Dark=#2A2A2A}" />
                            </Grid.Background>
                            
                            <!-- Index Again checkbox -->
                            <CheckBox Grid.Column="0"
                                      VerticalOptions="Center"
                                      Margin="0,0,8,0" />
                            
                            <!-- Folder path -->
                            <Label Grid.Column="1"
                                   Text="{Binding .}"
                                   VerticalOptions="Center"
                                   FontSize="14"
                                   LineBreakMode="MiddleTruncation" />
                            
                            <!-- Remove button -->
                            <Button Grid.Column="2"
                                    Text="✕"
                                    BackgroundColor="Transparent"
                                    TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray300}}"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type vm:LibrarySetupViewModel}}, Path=RemoveFolderCommand}"
                                    CommandParameter="{Binding .}"
                                    WidthRequest="36"
                                    HeightRequest="36" />
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>

        <!-- Action buttons -->
        <HorizontalStackLayout Grid.Row="2"
                               Margin="0,24,0,0"
                               Spacing="12"
                               HorizontalOptions="End">
            <Button Text="Cancel"
                    Clicked="OnCancelClicked"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InvertBoolConverter}}"
                    Style="{StaticResource SecondaryButtonStyle}" />
            <Button Text="Add Folder"
                    Command="{Binding AddFolderCommand}"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InvertBoolConverter}}"
                    Style="{StaticResource SecondaryButtonStyle}" />
            <Button Text="Done"
                    Clicked="OnDoneClicked"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InvertBoolConverter}}"
                    Style="{StaticResource PrimaryButtonStyle}" />
        </HorizontalStackLayout>

    </Grid>
</ContentPage>
